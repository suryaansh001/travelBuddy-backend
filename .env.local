# Server
NODE_ENV=development
PORT=3000

# Local Database (change password if needed)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/campusbuddy?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="local-development-jwt-secret-key-min-32"
JWT_EXPIRES_IN="24h"
JWT_REFRESH_SECRET="local-development-refresh-secret-32chars"
JWT_REFRESH_EXPIRES_IN="30d"

# CORS
CORS_ORIGIN="http://localhost:5173"

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="noreply@campusbuddy.local"
